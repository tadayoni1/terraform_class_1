# Add variables here. They should match with the ones from variables.tfvars. 
# In this terraform example it is redundant. But it is put here so you learn how variables are passed to different modules.
variable "EnvironmentName" {}
variable "aws_region" {}
